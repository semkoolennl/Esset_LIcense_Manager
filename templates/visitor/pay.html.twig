{% extends 'base.html.twig' %}

{% block title %}Order {{ product.name }}{% endblock %}

{% block body %}
<div class="example-wrapper">
    <h1>Order {{ product.name }}</h1>
    {{ dump(data)}}
    <a href={{ links.payment }}>Pay</a>
</div>
{% endblock %}
